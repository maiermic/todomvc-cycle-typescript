import { StreamAdapter } from '@cycle/base';
import { History, HistoryDriverOptions } from './interfaces';
export declare function makeHistoryDriver(history: History, options?: HistoryDriverOptions): (sink$: any, runSA: StreamAdapter) => any;
